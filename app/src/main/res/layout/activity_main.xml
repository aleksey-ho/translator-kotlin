<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
                name="viewModel"
                type="com.example.translator_kotlin.presentation.MainViewModel" />
    </data>

    <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                tools:context=".presentation.MainActivity"
                android:orientation="vertical">

            <androidx.viewpager2.widget.ViewPager2
                    android:id="@+id/viewPager"
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:layout_weight="1" />

            <com.google.android.material.tabs.TabLayout
                    android:id="@+id/tabLayout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:tabIndicatorColor="@color/colorPrimary"
                    app:tabMode="fixed"
                    app:tabGravity="fill" />

        </LinearLayout>

        <com.example.translator_kotlin.presentation.dialog.DialogShowingView
                android:layout_width="0dp"
                android:layout_height="0dp"
                app:dialogConfig="@{viewModel.dialogConfig}"
                app:dialogLayout="@layout/layout_dialog"
                app:dialogStyle="@style/Theme.AppCompat.Dialog"
                app:dialogViewModel="@{viewModel.dialogViewModel}"
                app:visibleOrGone="@{viewModel.isDialogVisible}"
                tools:ignore="MissingConstraints" />

    </RelativeLayout>

</layout>